using UnityEngine;

public class EvIlkAfterRuya : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Dialog.Instance.LoadAndStartDialog("EvIlkAfterRuya");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
