using UnityEngine;

public class EvIlkManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Dialog.Instance.LoadAndStartDialog("EvIlkVarýs");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
