using UnityEngine;

public class EvIkiAfterRuya : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Dialog.Instance.LoadAndStartDialog("EvIkýAfterRuya");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
